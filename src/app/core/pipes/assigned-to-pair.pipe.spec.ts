import { AssignedToPairPipe } from './assigned-to-pair.pipe';

describe('AssignedToPairPipe', () => {
  it('create an instance', () => {
    const pipe = new AssignedToPairPipe();
    expect(pipe).toBeTruthy();
  });
});
